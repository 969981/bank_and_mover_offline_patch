name: official-bank-recovery-fault-injection-ci

on:
  push:
    branches:
      - feature/official-bank-recovery-a-fault-injection
      - feature/official-bank-recovery-b-fault-injection
  workflow_dispatch:

jobs:
  fault-injector-host-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Recovery fault injector unit tests
        run: |
          cd bank/tools
          python test_build_recovery_fault_patch.py -v
